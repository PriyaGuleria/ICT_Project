<?php
namespace App\Model\Table;
use Cake\ORM\Table;
use Cake\Validation\Validator;
class BooksTable extends Table{
    // public function validationDefault(Validator $validator);
    // $validator->requirePresence("name","create","Name field should be needed");
    // $validator->requirePresence("email","create","Email field should be needed");
    // $validator->requirePresence("author","create","Author field should be needed");
}
?>